# TW 모델 렌파이 비주얼노벨 생성기 문제 해결 가이드

이 문서는 TW 모델 렌파이 비주얼노벨 생성기를 사용하면서 발생할 수 있는 일반적인 문제들과 그 해결책을 안내합니다.

## 🚀 서버 실행 문제

### 문제: 서버가 시작되지 않음

#### 증상
- `npm start` 실행 시 오류 메시지 표시
- `node start.js` 실행 시 오류 메시지 표시
- 포트가 이미 사용 중이라는 메시지 표시

#### 해결책

**1. Node.js 버전 확인**
```bash
node --version
```
- Node.js 14 이상이 필요합니다. 이전 버전이라면 업그레이드하세요.

**2. 포트 변경**
```bash
# 다른 포트로 실행
node start.js --port 8080
```

**3. 포트 사용 중인 프로세스 확인**
```bash
# Windows
netstat -ano | findstr :3000

# macOS/Linux
lsof -i :3000
```

**4. 방화벽 설정 확인**
- 방화벽이 포트 3000(또는 사용 중인 포트)를 차단하지 않는지 확인

### 문제: Python 서버 대안 사용 시 오류

#### 증상
- `python -m http.server` 실행 시 "command not found" 오류
- Python 2가 기본으로 실행됨

#### 해결책

**1. Python 3 설치 확인**
```bash
python3 --version
```

**2. Python 3로 명시적으로 실행**
```bash
python3 -m http.server 8000
```

**3. 가상 환경 사용 (권장)**
```bash
# 가상 환경 생성
python3 -m venv venv

# 가상 환경 활성화
# Windows
venv\Scripts\activate
# macOS/Linux
source venv/bin/activate

# 서버 실행
python -m http.server 8000
```

## 🌐 브라우저 관련 문제

### 문제: 페이지가 로딩되지 않거나 빈 화면 표시

#### 증상
- 브라우저에 빈 화면만 표시됨
- 일부 요소만 표시되고 나머지는 표시되지 않음
- JavaScript 오류가 콘솔에 표시됨

#### 해결책

**1. 브라우저 콘솔 확인**
- F12 키로 개발자 도구 열기
- Console 탭에서 오류 메시지 확인

**2. ES6 모듈 지원 확인**
- Chrome 61+, Firefox 60+, Edge 16+, Safari 10.1+ 필요
- 구버전 브라우저 사용 시 최신 버전으로 업데이트

**3. 로컬 파일 접근 문제**
- `file://` 프로토콜 대신 웹 서버를 통해 접속
- `http://localhost:3000` 또는 `http://localhost:8000` 사용

**4. CORS 오류**
- 웹 서버를 통해 접속하는지 확인
- 브라우저 보안 설정 확인

### 문제: 일부 기능이 작동하지 않음

#### 증상
- 버튼 클릭 시 반응 없음
- 모달이 열리지 않음
- 타임라인이 업데이트되지 않음

#### 해결책

**1. JavaScript 오류 확인**
- 브라우저 콘솔에서 오류 메시지 확인
- 특정 파일 로딩 실패 여부 확인

**2. 브라우저 캐시 삭제**
- Ctrl+Shift+R (Windows/Linux) 또는 Cmd+Shift+R (macOS)로 강력 새로고침
- 브라우저 설정에서 캐시 삭제

**3. 브라우저 확장 프로그램 비활성화**
- 광고 차단기 등 일부 확장 프로그램이 기능을 방해할 수 있음
- 시크릿 모드에서 테스트

## 📱 기능 관련 문제

### 문제: 스토리 정보 입력 후 다음 단계로 진행되지 않음

#### 증상
- "스토리 시작하기" 버튼 클릭 시 반응 없음
- 입력 폼 유효성 검사가 작동하지 않음

#### 해결책

**1. 필수 필드 확인**
- 제목, 주인공, 테마는 필수 항목
- 모든 필수 필드가 채워졌는지 확인

**2. 입력 데이터 유효성 확인**
- 특수 문자 제한 확인
- 입력 길이 제한 확인

**3. 브라우저 콘솔에서 오류 확인**
- `form submit` 이벤트 관련 오류 확인
- `story-controller.js` 관련 오류 확인

### 문제: AI 질문이 표시되지 않음

#### 증상
- 스토리 시작 후 질문이 표시되지 않음
- 질문 생성 중 로딩만 계속 표시됨

#### 해결책

**1. 질문 컨트롤러 확인**
- `question-controller.js` 파일이 올바르게 로드되었는지 확인
- `tone-question.js`, `character-action-question.js`, `dialogue-style-question.js` 파일 확인

**2. 컨텍스트 데이터 확인**
- 브라우저 콘솔에서 `storyController.currentPosition` 확인
- 컨텍스트 데이터가 올바른지 확인

**3. 비동기 처리 확인**
- `async/await` 처리 관련 오류 확인
- Promise 관련 오류 확인

### 문제: RPY 파일 생성 실패

#### 증상
- "RPY 내보내기" 버튼 클릭 시 오류 발생
- ZIP 파일 다운로드 실패
- 생성된 파일 내용이 올바르지 않음

#### 해결책

**1. 외부 라이브러리 확인**
- JSZip 라이브러리 로딩 확인
- FileSaver.js 라이브러리 로딩 확인
- 네트워크 탭에서 라이브러리 로딩 실패 여부 확인

**2. 스토리 상태 확인**
- 최소한의 이벤트가 생성되었는지 확인
- 스토리 흐름이 올바른지 확인

**3. 브라우저 다운로드 설정 확인**
- 브라우저 다운로드 설정 확인
- 다운로드 폴더 권한 확인

## 💾 데이터 관리 문제

### 문제: 저장/불러오기 기능이 작동하지 않음

#### 증상
- "저장" 버튼 클릭 시 데이터가 저장되지 않음
- "불러오기" 버튼 클릭 시 데이터가 복원되지 않음
- 페이지 새로고침 시 상태가 유지되지 않음

#### 해결책

**1. 브라우저 로컬 스토리지 확인**
- 브라우저 설정에서 로컬 스토리지 사용 가능 여부 확인
- 개인 정보 모드에서 테스트 시 일부 기능이 제한될 수 있음

**2. 데이터 크기 확인**
- 브라우저별 로컬 스토리지 크기 제한 확인
- 데이터가 너무 큰 경우 일부 기능이 제한될 수 있음

**3. 데이터 형식 확인**
- JSON 데이터 형식이 올바른지 확인
- `state-manager.js`에서 직렬화/역직렬화 관련 오류 확인

## 🔄 스토리 흐름 문제

### 문제: 병목 이동이 올바르지 않음

#### 증상
- 기 병목에서 승 병목으로 이동하지 않음
- 세그먼트 순서가 올바르지 않음
- 선택지가 다음 병목으로 수렴하지 않음

#### 해결책

**1. 흐름 컨트롤러 확인**
- `flow-controller.js` 파일의 병목 순서 확인
- 세그먼트 매핑이 올바른지 확인

**2. 병목 매퍼 확인**
- `bottleneck-mapper.js` 파일의 매핑 데이터 확인
- 병목과 세그먼트 관계가 올바른지 확인

**3. 상태 데이터 확인**
- `currentProgress` 데이터가 올바른지 확인
- `completedNodes` 데이터가 올바른지 확인

## 🐛 디버깅 팁

### 1. 브라우저 개발자 도구 활용

**콘솔 로그 확인**
```javascript
// 브라우저 콘솔에서 상태 확인
console.log(window.TWApp.storyController.getStoryState());
console.log(window.TWApp.stateManager.getStoryState());
```

**네트워크 탭 확인**
- JavaScript 파일 로딩 실패 여부 확인
- 외부 라이브러리 로딩 실패 여부 확인

**소스 탭 확인**
- 디버깅을 위해 중단점 설정
- 변수 값 추적

### 2. 코드 수정 시 테스트

**수정 후 강력 새로고침**
- Ctrl+Shift+R (Windows/Linux) 또는 Cmd+Shift+R (macOS)
- 브라우저 캐시 삭제 후 테스트

**점진적 테스트**
- 작은 단위로 기능 테스트
- 문제가 발생한 부분 집중적으로 테스트

### 3. 로컬 환경 변수 설정

**디버그 모드 활성화**
```javascript
// app.js 또는 관련 파일에서 디버그 모드 설정
const DEBUG_MODE = true;

if (DEBUG_MODE) {
    console.log('디버그 정보:', debugData);
}
```

## 📞 추가 지원

### 1. 오류 보고

오류를 보고할 때 다음 정보를 포함해주세요:

1. **사용 환경**
   - 운영체제 (Windows, macOS, Linux)
   - 브라우저 및 버전
   - Node.js 또는 Python 버전

2. **재현 단계**
   - 문제가 발생하는 정확한 단계
   - 입력 데이터 (가능한 경우)

3. **오류 메시지**
   - 브라우저 콘솔 오류 메시지
   - 서버 콘솔 오류 메시지

4. **스크린샷**
   - 문제가 발생하는 화면의 스크린샷
   - 개발자 도구 콘솔의 스크린샷

### 2. 커뮤니티 지원

- GitHub Issues: [프로젝트 Issues 페이지](https://github.com/your-username/tw-renpy-generator/issues)
- 포럼: [개발자 커뮤니티 포럼]

---

## 🔧 자주 묻는 질문 (FAQ)

### Q: 렌파이가 설치되어 있어야 하나요?
A: 아니요, 이 도구는 렌파이 프로젝트 파일을 생성하기만 하며, 생성된 파일을 실행하려면 별도로 렌파이를 설치해야 합니다.

### Q: 생성된 RPY 파일을 수정해도 되나요?
A: 네, 생성된 RPY 파일은 자유롭게 수정할 수 있습니다. 다만, 수정된 내용은 웹 인터페이스와 동기화되지 않습니다.

### Q: 모바일에서도 사용할 수 있나요?
A: 네, 반응형 디자인으로 구현되어 모바일에서도 기본적인 기능을 사용할 수 있습니다. 다만, 파일 다운로드는 일부 모바일 브라우저에서 제한될 수 있습니다.

### Q: 오프라인에서도 사용할 수 있나요?
A: 네, 한 번 로드된 후에는 오프라인에서도 기본적인 기능을 사용할 수 있습니다. 다만, 외부 라이브러리 로딩이 필요한 일부 기능은 제한될 수 있습니다.

---

이 문서가 문제 해결에 도움이 되지 않는 경우, GitHub Issues에 상세한 정보와 함께 문제를 보고해주세요.